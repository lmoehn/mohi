<%= form_for @signed_document do |f| %>
  <%= f.hidden_field :pdf_template_id %>
  <%= f.hidden_field :user_id %>
  <%= render partial: "pdf_templates/#{@signed_document.pdf_template.new_partial}", locals: {f: f} %>
  <%= submit_tag 'Approve', class: 'approved-button' %>
<% end %>
